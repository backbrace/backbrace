{
    "name": "PageTabMeta",
    "description": "Page tab meta data.",
    "type": "object",
    "properties": {
        "name": {
            "description": "Name of the tab.",
            "type": "string"
        },
        "text": {
            "description": "Caption of the tab.",
            "type": "string"
        },
        "pageName": {
            "description": "Display a subpage in this tab.",
            "type": "string"
        },
        "icon": {
            "description": "Tab icon.",
            "type": "string",
            "$ref": "icons.json"
        },
        "className": {
            "description": "Classes to add to the tab.",
            "type": "string"
        }
    },
    "required": [
        "name"
    ]
}